import React from "react";

export default function User() {
  return (
    <div>
      <h2>
        This User component will get only when you call from the parent using
        lazy concept
      </h2>
    </div>
  );
}
